apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: postgresql-barman-cloud-eso-identity
  namespace: postgres-db
subjects:
  - kind: ServiceAccount
    name: eso-identity
    namespace: postgres-db
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: postgresql-barman-cloud